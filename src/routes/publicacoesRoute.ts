import express from "express";

const router = express.Router();


// router.get("/Publicacoes", PublicacoesController.getAll); // Buscar pubs

// router.get("/Publicacoes", PublicacoesController.getPublicacao); // Buscar selecionada

// router.post("/Publicacoes", PublicacoesController.novaPublicacao); // nova pub / comparilhar pub

// router.put("/Publicacoes", PublicacoesController.editarPublicacao); //  editar / curtir

// router.delete("/Publicacoes", PublicacoesController.editarPublicacao); //  deletar

export default router;